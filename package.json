{
  "name": "keepical",
  "version": "0.1.0",
  "description": "Keepical — verlustarmer, rein clientseitiger ICS-Editor (raw/patch-basiert)",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "husky": "^9.1.7",
    "typescript": "^5.6.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
